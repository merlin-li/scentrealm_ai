# NutriSnap API 安全保护

## 身份和权限

保留微信服务端 code 换 openid 的身份验证。客户端不能指定可信用户 ID。会话使用随机 256 位 token，数据库仅保存 SHA-256 摘要，2 小时过期，退出即撤销。recognize 与 records 均校验会话。不要在小程序中嵌入共享 API 密钥或签名密钥：打包后的客户端密钥无法保密。

新增 NUTRISNAP_AI_ALLOWED_USER_IDS（服务端用户 UUID，逗号分隔）。留空表示向所有微信认证用户开放；内测应明确填写允许名单。NUTRISNAP_AI_ENABLED=false 可关闭 AI，保留手动记录。

## 调用费用和滥用

- 保留每用户每分钟 5 次、北京时间每天默认 50 次的数据库配额。
- 新增跨实例共享全站默认 30 次/60 秒、500 次/86400 秒上限。窗口从首个获准请求开始，不是自然日，也不是严格滚动窗口；跨窗口边界可能出现突发。
- 每用户 10 秒内最多一次；相同用户、相同图片字节 60 秒内最多一次。返回 429 和 Retry-After；不缓存识别结果，不是跨时间的幂等保证，重编码图片会改变摘要。
- 微信登录交换全站默认 120 次/分钟；记录接口每用户 120 次/分钟。
- 先鉴权、校验图片和配置，再扣用户配额、共享配额，最后调用模型。失败不退还已提交配额；共享限流拒绝也消耗个人配额，以保守控制重试。共享策略在同一事务全部通过才提交；用户配额和共享配额为两个事务。
- 数据库不可用、缺表或非法额度配置返回 503，不降级为无限调用。模型继续限制 1600 输出 token、35 秒超时、3 MB 图片、不自动重试。

次数预算不能保证精确账单上限，图像输入 token 仍有差异。需在模型供应商控制台另设预算和余额告警。当前无全站并发信号量，30 次/分钟和35秒超时限制正常调用压力，但不能代替供应商侧并发上限。

## 部署顺序

1. 在目标数据库执行现有迁移脚本 `node --env-file=.env.local scripts/nutrisnap-migrate.cjs`，新增 `nutrisnap_api_limits` 表；先迁移，再部署代码。
2. 按 `NUTRISNAP.env.example` 配置开关、允许名单及配额。建议内测先设置允许名单和较低的全站预算。
3. 部署后验证无 token / 过期 token 返回 401、非允许用户 403、重复请求 429，超过配额不触发模型。使用单独测试身份，避免真实模型费用。
4. 每日定时清理过期计数器（可分批）：`DELETE FROM nutrisnap_api_limits WHERE expires_at < UTC_TIMESTAMP(3) LIMIT 1000`，循环至无剩余。表仅保存策略/图片摘要和计数，不保存照片。
5. 在托管平台/WAF 配置 `/api/nutrisnap/*` 的 IP 限流与请求体大小控制，尤其 login 与未认证请求。当前应用层没有可信客户端 IP 来源配置，不信任任意 X-Forwarded-For。全局登录限流会牺牲部分攻击期间可用性；WAF 应在流量进入应用和数据库前拦截。

这次只修改 NutriSnap 路由，共用后端项目的其他 API 需另行审计。未执行线上迁移或发布。单元测试覆盖鉴权、拒绝时不调用模型、事务回滚等；未对真实 MySQL 做并发联调。
